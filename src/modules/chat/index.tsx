import React from 'react';
import { Fragment } from 'react';
import Logout from '../auth/logout';

const Chat = () => {
    return (
        <Fragment>
            This is Chat page
            <Logout />
        </Fragment>
    );
};

export default Chat;
